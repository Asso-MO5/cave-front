import { MediaLight } from './MediaLight.mjs'

/**
 * @class MediaLightList
 * @description Classe représentant une réponse de type MediaLightList.
 */
export class MediaLightList extends MediaLight {
  /**
   */
  constructor(props = {}) {
super(props);
  }

}
